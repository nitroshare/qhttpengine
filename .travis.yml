language: cpp

compiler:
  - gcc
  - clang

before_install:
  - sudo apt-get update -qq

install:
  - sudo apt-get install -qq qtbase5-dev

before_script:
  - mkdir build
  - cd build
  - cmake -DBUILD_TESTS=ON ..

script:
  - make && ctest
